services:
  postgres:
    image: postgres:18
    container_name: zero-postgres
    environment:
      POSTGRES_DB: zero
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"
    command: postgres -c wal_level=logical
