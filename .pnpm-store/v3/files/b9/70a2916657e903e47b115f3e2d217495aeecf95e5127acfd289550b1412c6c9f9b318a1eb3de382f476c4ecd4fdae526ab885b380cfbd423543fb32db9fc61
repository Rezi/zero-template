import type { LogContext } from '@rocicorp/logger';
/**
 * Convenience wrapper around a `node:inspector` {@link Session} for
 * optionally taking cpu profiles.
 */
export declare class CpuProfiler {
    #private;
    static connect(): Promise<CpuProfiler>;
    private constructor();
    start(): Promise<void>;
    stopAndDispose(lc: LogContext, filename: string): Promise<void>;
}
//# sourceMappingURL=profiler.d.ts.map