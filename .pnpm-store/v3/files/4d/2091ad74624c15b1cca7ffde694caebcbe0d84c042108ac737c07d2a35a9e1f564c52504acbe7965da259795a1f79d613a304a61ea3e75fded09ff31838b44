//#region ../replicache/src/call-default-fetch.ts
/**
* Helper function for {@link getDefaultPuller} and {@link getDefaultPusher}.
*/
async function callDefaultFetch(url, auth, requestID, requestBody) {
	const init = {
		headers: {
			"Content-type": "application/json",
			"Authorization": auth,
			"X-Replicache-RequestID": requestID
		},
		body: JSON.stringify(requestBody),
		method: "POST"
	};
	const request = new Request(url, init);
	const response = await fetch(request);
	const httpStatusCode = response.status;
	if (httpStatusCode !== 200) return [void 0, {
		httpStatusCode,
		errorMessage: await response.text()
	}];
	return [response, {
		httpStatusCode,
		errorMessage: ""
	}];
}
//#endregion
export { callDefaultFetch };

//# sourceMappingURL=call-default-fetch.js.map