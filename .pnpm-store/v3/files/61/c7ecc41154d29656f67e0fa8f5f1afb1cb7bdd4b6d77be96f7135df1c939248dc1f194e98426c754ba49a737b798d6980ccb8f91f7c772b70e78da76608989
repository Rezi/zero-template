import { maybeTransactionIsClosedRejection } from "./throw-if-closed.js";
//#region ../replicache/src/kv/read-impl.ts
var ReadImpl = class {
	#map;
	#release;
	#closed = false;
	constructor(map, release) {
		this.#map = map;
		this.#release = release;
	}
	release() {
		this.#release();
		this.#closed = true;
	}
	get closed() {
		return this.#closed;
	}
	has(key) {
		return maybeTransactionIsClosedRejection(this) ?? Promise.resolve(this.#map.has(key));
	}
	get(key) {
		return maybeTransactionIsClosedRejection(this) ?? Promise.resolve(this.#map.get(key));
	}
};
//#endregion
export { ReadImpl };

//# sourceMappingURL=read-impl.js.map