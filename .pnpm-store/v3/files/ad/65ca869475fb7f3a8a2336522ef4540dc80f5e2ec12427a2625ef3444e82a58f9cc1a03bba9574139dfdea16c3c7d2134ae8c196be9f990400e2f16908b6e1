//#region ../shared/src/parse-big-int.ts
function parseBigInt(val, radix) {
	const base = BigInt(radix);
	let result = 0n;
	for (let i = 0; i < val.length; i++) {
		result *= base;
		result += BigInt(parseInt(val[i], radix));
	}
	return result;
}
//#endregion
export { parseBigInt };

//# sourceMappingURL=parse-big-int.js.map