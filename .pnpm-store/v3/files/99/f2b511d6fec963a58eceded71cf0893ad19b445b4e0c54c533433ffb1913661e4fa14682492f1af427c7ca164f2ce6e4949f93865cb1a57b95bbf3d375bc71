import { useZero } from "./use-zero.js";
import { createEffect, createSignal, onCleanup } from "solid-js";
//#region ../zero-solid/src/use-connection-state.ts
/**
* Tracks the connection status of the current Zero instance.
*
* @returns The connection status of the Zero instance.
* @see {@link ConnectionState} for more details on the connection state.
*/
function useConnectionState() {
	const zero = useZero();
	const [connectionState, setConnectionState] = createSignal(zero().connection.state.current);
	createEffect(() => {
		onCleanup(zero().connection.state.subscribe(setConnectionState));
	});
	return connectionState;
}
//#endregion
export { useConnectionState };

//# sourceMappingURL=use-connection-state.js.map