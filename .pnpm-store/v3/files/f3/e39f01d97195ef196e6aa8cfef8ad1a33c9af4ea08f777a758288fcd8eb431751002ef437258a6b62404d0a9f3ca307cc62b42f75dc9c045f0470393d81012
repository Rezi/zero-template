import { unreachable } from "../../../shared/src/asserts.js";
import { maybeSplitAndPushEditChange } from "./maybe-split-and-push-edit-change.js";
//#region ../zql/src/ivm/filter-push.ts
function* filterPush(change, output, pusher, predicate) {
	if (!predicate) {
		yield* output.push(change, pusher);
		return;
	}
	switch (change[0]) {
		case 0:
		case 1:
			if (predicate(change[1].row)) yield* output.push(change, pusher);
			break;
		case 3:
			if (predicate(change[1].row)) yield* output.push(change, pusher);
			break;
		case 2:
			yield* maybeSplitAndPushEditChange(change, predicate, output, pusher);
			break;
		default: unreachable(change);
	}
}
//#endregion
export { filterPush };

//# sourceMappingURL=filter-push.js.map